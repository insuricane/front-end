import React from 'react';

const lineStyles = {
  display: 'block',
  width: '100%',
  height: '4px',
  background: 'rgba(0, 0, 0, 0.1)',
  marginBottom: '1rem',
};

const Line = () => (<div style={lineStyles} />);

export default Line;
